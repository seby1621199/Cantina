import { Typography } from '@mui/material';
import React, { useEffect, useState } from 'react';

function Admin() {

  return (
    <div style={{margin:20}}>
    <Typography>
      ADMIN
    </Typography>
    </div>
  );
}

export default Admin;
